# acme-api

Sample API for the SharedTerminal demo.

Try these commands:
- `npm test` — run the test suite
- `node src/index.js` — start the server
- `cat src/rate-limit.js` — read the rate limiter code
- `grep -r "TODO" src/` — find open tasks
